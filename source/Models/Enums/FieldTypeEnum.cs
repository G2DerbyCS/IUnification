namespace IUnification.Models.Enums
{
    /// <summary>
    /// An enum for use with an InputFeild instance to denote how the field should be handled in the user interface.
    /// </summary>
    public enum FieldType
    {
        Boolean   = 1,
        Standard  = 0,
        Sensitive = 2
    }
}
